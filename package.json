{
  "devDependencies": {
    "@percy/cli": "^1.29.0",
    "@percy/cypress": "^3.1.3-beta.0",
    "cypress": "^13.13.1",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "start": "http-server -p 8080",
    "test": "percy exec -- cypress run"
  }
}
